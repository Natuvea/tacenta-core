# note: The self-hosted runner, named only for a push to main.
name: t
on: push
permissions:
  contents: read
jobs:
  j:
    runs-on: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && fromJSON('["self-hosted", "tacenta-core"]') || 'ubuntu-latest' }}
    steps:
      - run: true
