# expect: can be scheduled on a self-hosted runner outside a push to main
name: t
on: push
permissions:
  contents: read
jobs:
  j:
    runs-on: ${{ github.event_name == 'pull_request' && fromJSON('["self-hosted"]') || 'ubuntu-latest' }}
    steps:
      - run: true
