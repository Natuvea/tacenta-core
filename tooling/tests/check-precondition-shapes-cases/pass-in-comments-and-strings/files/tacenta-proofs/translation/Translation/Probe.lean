import Translation.TacentaRatchet
open Aeneas Aeneas.Std
-- (hs : s + U32.max ≤ Usize.max)
/- (hs : s + U32.max ≤ Usize.max) -/
/-- docstring: s + U32.max ≤ Usize.max -/
def msg : String := "s + U32.max ≤ Usize.max"
def raw : String := r#"s + U32.max ≤ Usize.max"#
