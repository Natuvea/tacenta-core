# Gap register

Current register for assurance gate 2. Historical independent-reader reports stay
unchanged under `tacenta-test-vectors/runners/independent/`; this file records
the current disposition after later fixes.

Last assessed: 2026-09-17, with the implementation at `34c8636`.

## Status key

- **Closed:** resolved in current files, with evidence named here.
- **Open:** still needs a specification, model, vector, proof or tooling change.
- **Deferred:** accepted as outside the current month or outside the repository's
  present scope.
- **Historical:** accurate when reported, no longer a current gap.

## Gate class key

- **BLOCKING:** must close, or be changed by a recorded decision, before the
  second external proof-ledger engagement.
- **AMBIGUOUS:** must become closed, BLOCKING, NONBLOCKING or a recorded
  decision before gate 2 can be said to pass.
- **NONBLOCKING:** disclosed work that does not block the second engagement at
  the current recorded target.
- **DEFERRED:** outside the current completion scope only if a recorded decision
  states the target effect and revisit trigger.
- **CLOSED:** resolved in current evidence.

[ASSURANCE-OBLIGATIONS.md](ASSURANCE-OBLIGATIONS.md) records the component and
gate obligations that are not individual reader findings.

## Current items

| ID | Status | Gate class | Package | Area | Current disposition | Acceptance criterion |
| --- | --- | --- | --- | --- | --- | --- |
| HL-CLAIMS-01 | Open | OPEN | P8 | Proof and interoperability claims | The core front door now says which named zone is proved, that the public session API is tested rather than proved end to end, and that Tacenta's own wire format and KDF labels are not wire-compatible with Signal. Website PR #20 makes the same correction in the hero, FAQ, assurance copy, `llms.txt` and Open Graph card. The GitHub description carries the same verified-zone and compatibility scope. | Merge the core and website changes after green CI and recorded review, then re-read the public GitHub and site front doors against `CLAIMS.md`, `SECURITY.md` and `CONSTANTS.md`; no page may imply an end-to-end theorem or a completed interoperability run. |
| HL-FUZZ-01 | Closed | CLOSED | P3 | Fuzz reach and decoder-bound controls | `wire_decoders` has a committed 1,811-byte bundle that reaches acceptance, backed by a normal decode/re-encode test. `session_receive` mutates a genuine initial message and requires its committed zero-mutation seed to reach an accepted responder handshake. The ratchet and sparse-ratchet count tests now corrupt the actual fields in valid encodings and assert `Malformed`. | Satisfied: both fuzz targets replay the committed corpus; the accepted paths are required rather than inferred from input size. Disabling the ratchet skipped-count bound, the sparse-ratchet chains-count bound, or its skipped-count bound separately makes the corresponding test fail to complete within six seconds, while the restored checks pass immediately. |
| HL-FM-03 | Closed | CLOSED | P7 | Braid KEM refinement premise | At `34c8636`, `KemAgreesFor` no longer includes the unused `K.Correct` conjunct. `step_send_refines` and `step_receive_refines` now destructure only the two operation-agreement clauses they use; their wrapper theorems inherit the narrower boundary. `KemWitness.lean` proves the matching hypothesis shape is satisfiable without importing perfect correctness into it. | Satisfied: the changed theorem and witness plus every direct dependent and the axiom audit compile against the pinned cached dependencies; the attestation manifest is current. The four Braid refinement theorems no longer assume perfect KEM correctness, while `Model.Braid.toyKem_correct` remains a separate model fact. |
| HL-FM-04 | Closed | CLOSED | P7/P8 | Refinement claim scope | The root README, assurance summary, ADR-0008 level definition and proof README now define T3 as named, scoped functional agreement rather than an unqualified whole-domain refinement. `CLAIMS.md` says directly that the classical Double Ratchet and composed Triple Ratchet `receive` theorems cover successful Rust results only, while entries with stronger accepted/refused matching say so individually. | Satisfied for claim accuracy: no summary treats a T3 badge as bidirectional, whole-domain or refusal-branch refinement. Proving further `Err`-branch and acceptance-completeness statements remains useful future proof work, but is not implied by the current assurance level. |
| HL-FM-01 | Closed | CLOSED | P7 | No-sorry gate | `Model.AxiomAudit` calls Lean's `collectAxioms` for every first-party declaration and refuses any dependency on `sorryAx`, independently of diagnostic settings. `check-lean-constructs.sh` also refuses `set_option warn.sorry false` across generated and hand-written Lean. The planted `sorry-with-warning-disabled` case suppresses the warning and must still be refused specifically for `sorryAx`. | Satisfied: the real model build audits 3,468 first-party declarations with no `sorryAx` dependency; the negative suite demonstrates that silencing the warning does not silence the gate. Aeneas's four dependency sorries remain disclosed, and a first-party declaration reaching one would now fail. |
| HL-FM-02 | Open | OPEN | P7/P8 | Numeric-precondition satisfiability | `LIMITATIONS.md` records about 140 numeric bound hypotheses on about 95 theorems in about 54 shapes. The component table now marks the sparse ratchet, Triple Ratchet, Braid and erasure rows with `*`, and ADR-0008 says an assurance level does not itself establish premise satisfiability. Existing invariant chains discharge the classical ratchet's store bounds and the sparse ratchet's room bounds, but families including `epochsKept` and erasure `32 *` bounds remain unexamined. | For each pinned headline theorem, add a kernel-checked example applying it to a concrete jointly satisfying hypothesis set, and have the manifest map that witness to the theorem. Remove a component's `*` only after every numeric hypothesis set behind its headline claims is covered; separately state which reachable states meet each premise. |
| HL-PROCESS-01 | Open | OPEN | P8/P9 | Independent review | In the audited post-ADR scope, 32 merged normative PRs (#77–#98, #100–#103, #105–#107, #109, #110 and #129) have neither a review nor a comment. Every other recorded review was submitted by the change author's account. Since 2026-09-17, `main` protection requires one approving account other than the last pusher, dismisses stale approvals on a push, requires conversations resolved and applies to administrators. Automated or AI-assisted analysis remains project-controlled evidence and does not satisfy that approval. | Demonstrate the live rule on the current remediation stack and retain an independent, claim-by-claim approval on its frozen head before merge. |
| HL-SC-03 | Historical | CLOSED | P9 | CI on merged heads | Thirteen merged PR heads (#76–#81, #100, #101 and #103–#107) have neither a check run nor an Actions run recorded. This evidence cannot be reconstructed retroactively. Since 2026-09-17, `main` protection requires the ten current contexts on an up-to-date PR head, applies to administrators, requires linear history and blocks force-pushes and deletion. | Recurrence prevention is enforced by the live repository rule; retain the historical disclosure and require hosted evidence on each future merge head. |
| HL-SC-04 | Open | OPEN | P9 | Push assurance aggregation | The collector now uses `always()` so a skipped pull-request-only `sign-off` dependency does not automatically skip the job on a push. The prior `main` runs were skipped, and the fixed push path has not yet completed on `main`. | After merge, retain a `main` run in which `assurance-receipts` concludes success and its artifact binds that exact head. |
| HL-SC-05 | Open | OPEN | P9 | Receipt authenticity | Per-job checkpoints and their aggregate remain unsigned JSON written by the candidate workflow, and their literal `status: pass` means only that the post-check step was reached. `hosted-assurance.yml` now runs trusted default-branch code after CI, reads the completed run and ten job conclusions from GitHub's API, refuses a different head/event, missing or duplicate jobs and non-success required conclusions, and never checks out the candidate. Five local cases cover PR and push success plus missing-job, failure and wrong-head refusals. | After the workflow lands on `main`, retain a successful hosted-conclusion artifact and bind it into the final evidence validator and pack; until then the live provenance path has not been demonstrated. |
| HL-SC-06 | Closed | CLOSED | P9 | Composite-action policy | `check-workflows.sh` now walks `**/.github/actions/**/action.y*ml`, applies action pin, checkout-credential and download-execution rules to composite steps, and has focused failing cases for an unpinned action and `curl | bash`. Repository-level Actions SHA pinning is also enabled. | Keep both the cases and the live repository setting; any new action form must enter the same scan. |
| HL-IP-01 | Open | OPEN | P1/P9 | Legacy profile provenance | Six `nominated` value families were attributed to black-box observation of a pinned build, but no package/version, transcript or ADR-0003 research record exists in the public or retained project records; the conformance manifest separately says the libsignal run remains undone. The unsupported observation claims have been removed and each row now states that provenance is unresolved. | Do not reconstruct history from memory. Either produce a new, dated ADR-0003 experiment against an exact package/build with public or hash-committed records, or retire the affected interoperability-profile values and claims. |
| HL-IP-02 | Open | OPEN | P1/P9 | Protobuf semantic-label provenance | The protobuf field numbers, wire types and semantic labels are legacy profile inputs. The prior claim that the names were independently chosen was unsupported, especially for `registration_id`, which no pinned protocol specification defines. The spec and code now describe both encoding and labels as unresolved legacy provenance. | Have a party permitted to inspect the third-party API/schema trace each overlap and publish its disposition; then rename, attribute or retain each label on recorded grounds. |
| HL-IP-03 | Open | OPEN | P1/P9 | Tool and role provenance | The public history has one maintainer account. ADR roles are now described as information-flow roles rather than separate teams, and the README discloses automated and AI-assisted contributions without individual tool attribution. The project cannot establish that model training excluded third-party expression. | Obtain a similarity review from a party permitted to inspect both codebases and retain its method, reviewed revisions and findings; keep tool output classified as project-controlled evidence. |
| HL-IP-04 | Open | OPEN | P9 | Third-party reader licence | The tree says Jie Sun independently authored `session-operation-reader.py`, but its importing commit carries only the maintainer's DCO sign-off and the public tree contains no authorship/licence grant from Jie Sun. The evidence can be cited as a delivered result, but its redistribution licence is not evidenced. | Obtain and retain Jie Sun's DCO sign-off or an explicit Apache-2.0 grant/assignment and relationship statement; otherwise replace/remove the file and narrow the evidence claim. |
| HL-IP-06 | Closed | CLOSED | P1 | Neutral RPC vocabulary | The neutral adapter document formerly denied reproducing libsignal's API while using `PreKeyBundle` and `process_prekey_bundle`. It now makes the narrower supportable statement: the RPC is not an API-compatibility claim, and these names follow common Signal-ecosystem vocabulary rather than being independently coined. | Keep the distinction between shared vocabulary, the Tacenta RPC contract and any future API-compatibility claim explicit. |
| HL-SC-15 | Closed | CLOSED | P9 | Repository security settings | Repository-level Actions SHA pinning, secret scanning, push protection, secret validity checks, Dependabot security updates and private vulnerability reporting are enabled. `SECURITY.md` links the private reporting channel. | Retain the live settings and re-check them during release readiness review. |
| HL-SC-14 | Closed | CLOSED | P9 | Empty-input gates | The workflow checker and actionlint wrapper now fail when no workflow exists; the workflow case runner already fails when no fixture exists and now exercises an empty repository explicitly. Vector currency checks use `git status --porcelain --untracked-files=all`, so a newly generated untracked vector fails both local and hosted gates. | Keep the empty-tree control and untracked-file check; additions to generated evidence paths need equivalent status-based currency checks. |
| HL-SC-10 | Closed | CLOSED | P9 | DCO sign-off gate | `check-signoff.sh` now fails when its required base is absent and checks merge commits as well as ordinary commits. Its production case runner requires a signed commit to pass and plants missing-base, unsigned-commit and unsigned-merge refusals. Protected `main` also requires linear history, so feature branches should rebase rather than add merge commits. | Keep the three refusal controls and fetch the exact pull-request base before invoking the gate. |
| HL-IMP-01 | Closed | CLOSED | P3/P4/P6 | Session known answer | At `b4fdeff`, `session-e2e.json` fixes every random draw and pins a real X25519/ML-KEM-1024 prekey bundle, all four DH results, the PQXDH and split secrets, first ratchet keys, associated data, AEAD and wire bytes, responder plaintext, consumed prekey store and both session states. The runner requires its public lifecycle and separate public-component reconstruction to produce the same initial message. A negative control changes the expected associated data and requires failure. Independent-reader pass 10 reports this real-ML-KEM vector as one explicit skip because that reader implements only its documented KEM test double. | Satisfied for the reported missing byte-level vector: a deterministic real-primitive handshake and first message are pinned end to end, with a control proving the expected fields are checked. The project-generated expected bytes are disclosed as regression/composition evidence, not an external oracle or independent review. |
| HL-IMP-02 | Closed | CLOSED | P3/P5/P7 | Double Ratchet replacement bound | The normative order is recorded in `CHANGELOG.md`. At `3e2745f`, the model and Rust remove replaceable pairs before checking the resulting store length. `replacement-bound-counts-resulting-store` pins a 1,999-key state at byte level, the differential harness requires that boundary to be reached, the translated T1/T3 proofs were regenerated and rebuilt, and independent-reader pass 9 records 646 PASS, 0 FAIL, 0 SKIP. | Satisfied: normative resolution, model/Rust agreement, distinguishing vector and differential sequence, translated proofs, and a new reader pass are all recorded. |
| G7-01 | Closed | CLOSED | P1 | Vector layouts | `tacenta-test-vectors/README.md` now states the `prekey-store-state.json` and `session-state.json` `fields` layouts, including the derived `braid_tag`, `braid_epoch` and `sparse_epoch` session names. | Independent reader can implement the two files' exact field names from the README layout rather than from inference. |
| G7-02 | Closed | CLOSED | P1 | Vector status | The README status paragraph now says the session and prekey-store persisted formats have vectors, and that every persistence file except the two erasure coders names its refusal. | No current status paragraph says those two files have no vectors or limits refusals to the two ratchet-state files. |
| G7-03 | Closed | CLOSED | P3 | Prekey signatures | `session-persistence.md` now says stored prekey signatures verify using the unlabelled prekey signature input. `CHANGELOG.md` records the clarification. | A reader no longer has to choose between labelled application signatures and unlabelled prekey signatures. |
| G7-04 | Closed | CLOSED | P3 | Prekey signing operations | `session-persistence.md` now names `create_prekeys`, `replenish`, `rotate_signed_prekey` and `rotate_kem` as the operations covered by the signing obligation. `CHANGELOG.md` records the clarification. | A reader no longer has to infer whether the obligation reaches only rotations or every operation that signs a prekey. |
| G7-05 | Closed | CLOSED | P3 | Prekey store rule interaction | `session-persistence.md` now records this as a wording-only rule interaction: `identity_public = p - 1` passes the canonical curve-key rule, but signature verification refuses that value before converting it to an Edwards point, so the stored-signature rule intentionally narrows the accepted set. | `cases_signed.py` PK-03 and `cases_stored.py` SK-08 keep the independent reader's distinction between canonical-key refusal and the later signature-rule refusal. `prekey-store-state/signed-prekey-signature-does-not-verify` now also pins the later signature-rule refusal with a cryptographic runner verdict outside the model boundary. |
| G7-06 | Closed | CLOSED | P1 | Session vector comment | The generator comment for `sparse-epoch-does-not-follow-the-braid` now says the Braid epoch is moved outside the relation, rather than saying the sparse epoch moved. Regenerated vectors carry the corrected comment. | The vector's comment names the half that actually changed, and no runner behavior changes. |
| G5-03 | Closed | CLOSED | P3 | Persistence refusal precedence | `session-persistence.md` now keeps the implementation freedom deliberately, but scopes it to the exact overlap where a non-empty buffer is too short for every recognised version and its first byte is unknown; a format-by-format table names the six affected persisted formats. | `cases_stored.py` RJ-01 checks that the empty buffer is short, a recognised-version truncation is short-or-malformed, a long enough unknown-version buffer is wrong-version, and only the short unknown-version overlap accepts either refusal. |
| G5-09 | Closed | CLOSED | P3 | Ratchet refusal ordering | `ratchet.md` now records deliberate implementation freedom for the exact overlap where `Nr = u32::MAX` and a same-chain unstored message has a lower number: stale/out-of-order and `ChainExhausted` are both conforming refusals, while accepting is not. | `cases_ratchet.py` CR-20 checks the overlap and cites CR-02 and CR-04 as adjacent cases: message number `u32::MAX` remains exhaustion, and same-chain unstored messages below `Nr < u32::MAX` remain stale. |
| G4-01 | Closed | CLOSED | P3 | Composite header trailing bytes | `message-format.md` now makes standalone composite-header decoding a public contract for vectors, associated-data construction and implementations that parse the header before ciphertext: it accepts exactly 102 bytes and refuses trailing bytes. | `negative_cases.py` RM-14 checks that the standalone composite-header decoder rejects bytes after the fixed-width header, while the vector README records why the ratchet-message decoder still gives the existing 102-byte decoder vectors the same verdict. |
| G6-01 | Closed | CLOSED | P3 | Vector naming | `tacenta-test-vectors/README.md` now documents that `sk` in `ratchet-state.json` and `sparse-ratchet-state.json` is the already split per-ratchet initial root secret, while `sk` in `triple-ratchet-state.json` is the unsplit Triple Ratchet shared secret. | A runner can implement the layouts without inferring whether it must split `sk` before initialising the Double and Sparse Ratchets. The vector field names are left stable. |
| G6-03 | Closed | CLOSED | P3/P7 | Braid operation read-back | `tacenta-test-vectors/README.md` now states that Braid operation `output` must read back and write as the same bytes. | The independent reader's `h_braid_state` handler asserts `output read back and written again`, so the Braid operation vectors use an equivalent runner check rather than separate `-read-back` siblings. |
| G6-05 | Closed | CLOSED | P3/P7 | Braid absent codeword encoding | `tacenta-test-vectors/README.md` now states that Braid operation steps use the wire Braid-message fields, so an absent codeword has all-zero `chunk_index` and `chunk` padding. | The independent reader's vector handler already reports an absent codeword with non-zero index or chunk data as malformed vector data, so the runner check is explicit and matches the layout rule. |
| PK-OLD-VERSIONS | Closed | CLOSED | P4/P5 | Persisted prekey coverage | `prekey-store-state.json` now includes accepted `legacy-v1`, `legacy-v2` and `legacy-v3` fixtures derived from the no-record, no-retired v4 fixture; the independent reader checks their fields and permits the required v4 upgrade on write-back. | Older-version prekey-store parsing is pinned by vectors rather than implementation tests alone. `PK-PREVIOUS-KEM` is now closed by the separate retired-KEM fixture. |
| PK-PREVIOUS-KEM | Closed | CLOSED | P4/P5 | Persisted prekey coverage | `prekey-store-state.json` now includes the accepted `retired-kem-prekey` fixture produced by `print_prekey_store_fixtures`; it carries `previous_kem_present = 0x01`, and the Rust vector runner walks `len(4) || kem_pair || id(4) || sig(64)` before requiring the store to end. | The prekey store's retired-KEM sub-format is pinned by a deterministic fixture; the stored-signature refusal is closed separately by `PK-SIGNATURE-REFUSAL`. |
| PK-SIGNATURE-REFUSAL | Closed | CLOSED | P4 | Prekey signature refusal | `prekey-store-state.json` now includes `signed-prekey-signature-does-not-verify`, a one-byte mutation of `signed_prekey_sig` in a valid fixture. The generator first requires the model to accept and re-encode the mutated bytes, so parser, KEM-pair and structural semantic checks are not the masking refusal. | The cryptographic runners refuse the fixture as `incoherent`, pinning the stored-signature refusal that remains outside the model's signature-free boundary. |
| SESS-BRAID-TAG-BOUNDARY | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes accepted neighbours on both sides of the boundary: `tag-six-keeps-previous-sparse-epoch` and `tag-seven-uses-current-sparse-epoch`. | The refused siblings `tag-six-with-current-sparse-epoch-refused` and `tag-seven-with-previous-sparse-epoch-refused` reach `inconsistent`, so vectors now distinguish the tag 6 `e - 1` branch from the tag 7 `e` branch. |
| SESS-FAILED-BRAID | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes `failed-braid-exempts-sparse-epoch`, an accepted session with Braid tag 11 and sparse epoch 1. | The fixture pins that Failed carries no epoch and is exempt from the live-Braid epoch relation; a runner that applies the relation to Failed rejects it. |
| SESS-BRAID-ROLE | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes `halves-disagree-on-the-braid-role`, a tag 6 / epoch 2 responder fixture whose sparse epoch still follows the Braid. | The vector reaches `inconsistent` only through the Braid tag/parity role relation; the sparse-role sibling remains `halves-disagree-on-the-sparse-role`. |
| SESS-RATCHET-PRIVATE | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes `ratchet-private-does-not-match-dhs-pub`, appended by `augment-session-state.py` from the responder fixture after the Lean generator runs. | The vector flips byte 7 of `ratchet_private`, away from X25519 clamp bits, and both the independent and Rust readers refuse it as `inconsistent`, covering the rule the Lean model cannot compute. |
| SESS-UNANSWERED-ROLE | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes `unanswered-initiator-is-also-responder`, generated by adding the valid `pending_initial` from the unanswered initiator fixture to the responder fixture. | The vector remains well-formed and canonical, keeps the responder role through `established_ephemeral`, and reaches `inconsistent` only because an unanswered initiator is also a responder. |
| SESS-INNER-INVARIANTS | Closed | CLOSED | P4/P5 | Session coverage | `session-state.json` now includes `triple-state-reader-refuses` and `braid-reader-refuses`, each changing the nested half's own version byte inside the responder fixture. | The session reader refuses both as `short-or-malformed`, pinning that a half its own reader refuses is rejected at the session boundary before session semantic rules run and without passing through the nested `wrong-version`. |
| TRACE-EVIDENCE-INDEX | Closed | CLOSED | P2 | Tooling | `tooling/check-traceability.py` now enforces the structural spine and migrated evidence-index links for every security-property requirement. The checked `security-properties/evidence-index.json` covers all 32 authentication, confidentiality, forward-secrecy and post-compromise-security requirements, including proof, claim, vector, test and explicit missing-evidence references where present. `tooling/tests/run-check-traceability-cases.sh` holds that gate to pass and eleven refusal cases, including a confidentiality vector mutation and a missing-requirement-entry mutation. | The P2 index is complete for the current requirement set; future requirement additions must add evidence-index entries or the traceability gate fails. |
| PROOF-BOUNDARY-HEADROOM | Deferred | DEFERRED | P7 | Proof scope | [PROOF-BOUNDARY-HEADROOM-TARGET-DECISION.md](tacenta-proofs/PROOF-BOUNDARY-HEADROOM-TARGET-DECISION.md) keeps the successor-headroom premises as the scope of the existing success-refinement targets. Ceiling refusals remain evidence of refusal behaviour, not a boundary-success refinement claim. | Reopen on a changed ceiling/refusal/model transition, a theorem or target that needs the boundary success case, or a review request; then prove matching refusal refinement or restate/prove the success theorems without the premise. |
| PROOF-ERASURE-REFINEMENT | Deferred | DEFERRED | P7 | Proof scope | [ERASURE-CODEC-TARGET-DECISION.md](tacenta-proofs/ERASURE-CODEC-TARGET-DECISION.md) sets L3 as the current engagement target. Field arithmetic and panic-freedom are covered; full codec refinement remains outside that evidence. `ERASURE-DECODER-REFINEMENT.md` records the opaque-boundary gap: duplicate/completed model effects and the completed translated leaf-decoder effect are proved, but Braid's erasure dependency remains separately translated and opaque. | Reopen the L4 programme on a recorded trigger, then link the leaf and Braid symbols, bridge duplicate and completed observations through the real translated decoder API, and prove concrete `DecoderRefines` preservation. |

## Notes

- G5-02 is historical and closed: the Braid `key_pair` content clause is scoped
  to implementations that know the delegated KEM layout.
- G5-07 is narrowed by ADR-0006 point 7: evidence may be cited from outside the
  specification, but normative content must be readable from the specification.
- Historical reports should not be edited to match this register. Add a row here
  when later work changes an item's disposition.
