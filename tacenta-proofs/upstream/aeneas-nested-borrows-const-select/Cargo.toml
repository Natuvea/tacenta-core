[package]
name = "nested-borrows-const-select"
version = "0.0.0"
edition = "2021"
# Upstream reproducer, never published, as `SECURITY.md` states for every
# crate here.
publish = false

[dependencies]
