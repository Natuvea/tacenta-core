-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [tacenta_protobuf]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

namespace tacenta_protobuf

/-- [tacenta_protobuf::MAX_MESSAGE_LEN]
    Source: 'protobuf/src/lib.rs', lines 33:0-33:41
    Visibility: public -/
@[global_simps, irreducible] def MAX_MESSAGE_LEN : Std.Usize := 16384#usize

/-- [tacenta_protobuf::MAX_FIELD_NUMBER]
    Source: 'protobuf/src/lib.rs', lines 38:0-38:37
    Visibility: public -/
@[global_simps, irreducible] def MAX_FIELD_NUMBER : Std.U32 := 15#u32

/-- [tacenta_protobuf::MAX_FIELDS]
    Source: 'protobuf/src/lib.rs', lines 42:0-42:33
    Visibility: public -/
@[global_simps, irreducible] def MAX_FIELDS : Std.Usize := 32#usize

/-- [tacenta_protobuf::MAX_VARINT_BYTES]
    Source: 'protobuf/src/lib.rs', lines 47:0-47:38
    Visibility: public -/
@[global_simps, irreducible] def MAX_VARINT_BYTES : Std.Usize := 5#usize

/-- [tacenta_protobuf::ProtoError]
    Source: 'protobuf/src/lib.rs', lines 54:0-66:1
    Visibility: public -/
@[discriminant isize]
inductive ProtoError where
| TooLong : ProtoError
| Truncated : ProtoError
| BadVarint : ProtoError
| NotInProfile : ProtoError
| Duplicate : ProtoError

/-- [tacenta_protobuf::{impl core::clone::Clone for tacenta_protobuf::ProtoError}::clone]:
    Source: 'protobuf/src/lib.rs', lines 53:9-53:14
    Visibility: public -/
def ProtoError.Insts.CoreCloneClone.clone
  (self : ProtoError) : Result ProtoError := do
  ok self

/-- Trait implementation: [tacenta_protobuf::{impl core::clone::Clone for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:9-53:14 -/
@[reducible]
def ProtoError.Insts.CoreCloneClone : core.clone.Clone ProtoError := {
  clone := ProtoError.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [tacenta_protobuf::{impl core::marker::Copy for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:16-53:20 -/
@[reducible]
def ProtoError.Insts.CoreMarkerCopy : core.marker.Copy ProtoError := {
  cloneInst := ProtoError.Insts.CoreCloneClone
}

/-- Trait implementation: [tacenta_protobuf::{impl core::marker::StructuralPartialEq for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:22-53:31 -/
@[reducible]
def ProtoError.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq ProtoError := {
}

/-- [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::ProtoError> for tacenta_protobuf::ProtoError}::eq]:
    Source: 'protobuf/src/lib.rs', lines 53:22-53:31
    Visibility: public -/
def ProtoError.Insts.CoreCmpPartialEqProtoError.eq
  (self : ProtoError) (other : ProtoError) : Result Bool := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::ProtoError> for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:22-53:31 -/
@[reducible]
def ProtoError.Insts.CoreCmpPartialEqProtoError : core.cmp.PartialEq ProtoError
  ProtoError := {
  eq := ProtoError.Insts.CoreCmpPartialEqProtoError.eq
}

/-- [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::ProtoError}::assert_fields_are_eq]:
    Source: 'protobuf/src/lib.rs', lines 53:33-53:35
    Visibility: public -/
def ProtoError.Insts.CoreCmpEq.assert_fields_are_eq
  (self : ProtoError) : Result Unit := do
  ok ()

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:33-53:35 -/
@[reducible]
def ProtoError.Insts.CoreCmpEq : core.cmp.Eq ProtoError := {
  partialEqInst := ProtoError.Insts.CoreCmpPartialEqProtoError
  assert_fields_are_eq := ProtoError.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::ProtoError}::fmt]:
    Source: 'protobuf/src/lib.rs', lines 53:37-53:42
    Visibility: public -/
def ProtoError.Insts.CoreFmtDebug.fmt
  (self : ProtoError) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | ProtoError.TooLong => core.fmt.Formatter.write_str f (toStr "TooLong")
  | ProtoError.Truncated => core.fmt.Formatter.write_str f (toStr "Truncated")
  | ProtoError.BadVarint => core.fmt.Formatter.write_str f (toStr "BadVarint")
  | ProtoError.NotInProfile =>
    core.fmt.Formatter.write_str f (toStr "NotInProfile")
  | ProtoError.Duplicate => core.fmt.Formatter.write_str f (toStr "Duplicate")

/-- Trait implementation: [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::ProtoError}]
    Source: 'protobuf/src/lib.rs', lines 53:37-53:42 -/
@[reducible]
def ProtoError.Insts.CoreFmtDebug : core.fmt.Debug ProtoError := {
  fmt := ProtoError.Insts.CoreFmtDebug.fmt
}

/-- [tacenta_protobuf::WireType]
    Source: 'protobuf/src/lib.rs', lines 70:0-73:1
    Visibility: public -/
@[discriminant isize]
inductive WireType where
| Varint : WireType
| LengthDelimited : WireType

/-- [tacenta_protobuf::{impl core::clone::Clone for tacenta_protobuf::WireType}::clone]:
    Source: 'protobuf/src/lib.rs', lines 69:9-69:14
    Visibility: public -/
def WireType.Insts.CoreCloneClone.clone
  (self : WireType) : Result WireType := do
  ok self

/-- Trait implementation: [tacenta_protobuf::{impl core::clone::Clone for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:9-69:14 -/
@[reducible]
def WireType.Insts.CoreCloneClone : core.clone.Clone WireType := {
  clone := WireType.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [tacenta_protobuf::{impl core::marker::Copy for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:16-69:20 -/
@[reducible]
def WireType.Insts.CoreMarkerCopy : core.marker.Copy WireType := {
  cloneInst := WireType.Insts.CoreCloneClone
}

/-- Trait implementation: [tacenta_protobuf::{impl core::marker::StructuralPartialEq for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:22-69:31 -/
@[reducible]
def WireType.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq WireType := {
}

/-- [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::WireType> for tacenta_protobuf::WireType}::eq]:
    Source: 'protobuf/src/lib.rs', lines 69:22-69:31
    Visibility: public -/
def WireType.Insts.CoreCmpPartialEqWireType.eq
  (self : WireType) (other : WireType) : Result Bool := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::WireType> for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:22-69:31 -/
@[reducible]
def WireType.Insts.CoreCmpPartialEqWireType : core.cmp.PartialEq WireType
  WireType := {
  eq := WireType.Insts.CoreCmpPartialEqWireType.eq
}

/-- [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::WireType}::assert_fields_are_eq]:
    Source: 'protobuf/src/lib.rs', lines 69:33-69:35
    Visibility: public -/
def WireType.Insts.CoreCmpEq.assert_fields_are_eq
  (self : WireType) : Result Unit := do
  ok ()

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:33-69:35 -/
@[reducible]
def WireType.Insts.CoreCmpEq : core.cmp.Eq WireType := {
  partialEqInst := WireType.Insts.CoreCmpPartialEqWireType
  assert_fields_are_eq := WireType.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::WireType}::fmt]:
    Source: 'protobuf/src/lib.rs', lines 69:37-69:42
    Visibility: public -/
def WireType.Insts.CoreFmtDebug.fmt
  (self : WireType) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | WireType.Varint => core.fmt.Formatter.write_str f (toStr "Varint")
  | WireType.LengthDelimited =>
    core.fmt.Formatter.write_str f (toStr "LengthDelimited")

/-- Trait implementation: [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::WireType}]
    Source: 'protobuf/src/lib.rs', lines 69:37-69:42 -/
@[reducible]
def WireType.Insts.CoreFmtDebug : core.fmt.Debug WireType := {
  fmt := WireType.Insts.CoreFmtDebug.fmt
}

/-- [tacenta_protobuf::Tag]
    Source: 'protobuf/src/lib.rs', lines 87:0-90:1
    Visibility: public -/
structure Tag where
  field : Std.U32
  wire : WireType

/-- [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::Tag}::fmt]:
    Source: 'protobuf/src/lib.rs', lines 86:9-86:14
    Visibility: public -/
def Tag.Insts.CoreFmtDebug.fmt
  (self : Tag) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.field
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugShared WireType.Insts.CoreFmtDebug) self.wire
  core.fmt.Formatter.debug_struct_field2_finish f (toStr "Tag") (toStr "field")
    dyn (toStr "wire") dyn1

/-- Trait implementation: [tacenta_protobuf::{impl core::fmt::Debug for tacenta_protobuf::Tag}]
    Source: 'protobuf/src/lib.rs', lines 86:9-86:14 -/
@[reducible]
def Tag.Insts.CoreFmtDebug : core.fmt.Debug Tag := {
  fmt := Tag.Insts.CoreFmtDebug.fmt
}

/-- Trait implementation: [tacenta_protobuf::{impl core::marker::StructuralPartialEq for tacenta_protobuf::Tag}]
    Source: 'protobuf/src/lib.rs', lines 86:16-86:25 -/
@[reducible]
def Tag.Insts.CoreMarkerStructuralPartialEq : core.marker.StructuralPartialEq
  Tag := {
}

/-- [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::Tag> for tacenta_protobuf::Tag}::eq]:
    Source: 'protobuf/src/lib.rs', lines 86:16-86:25
    Visibility: public -/
def Tag.Insts.CoreCmpPartialEqTag.eq
  (self : Tag) (other : Tag) : Result Bool := do
  if self.field = other.field
  then WireType.Insts.CoreCmpPartialEqWireType.eq self.wire other.wire
  else ok false

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::PartialEq<tacenta_protobuf::Tag> for tacenta_protobuf::Tag}]
    Source: 'protobuf/src/lib.rs', lines 86:16-86:25 -/
@[reducible]
def Tag.Insts.CoreCmpPartialEqTag : core.cmp.PartialEq Tag Tag := {
  eq := Tag.Insts.CoreCmpPartialEqTag.eq
}

/-- [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::Tag}::assert_fields_are_eq]:
    Source: 'protobuf/src/lib.rs', lines 86:27-86:29
    Visibility: public -/
def Tag.Insts.CoreCmpEq.assert_fields_are_eq (self : Tag) : Result Unit := do
  ok ()

/-- Trait implementation: [tacenta_protobuf::{impl core::cmp::Eq for tacenta_protobuf::Tag}]
    Source: 'protobuf/src/lib.rs', lines 86:27-86:29 -/
@[reducible]
def Tag.Insts.CoreCmpEq : core.cmp.Eq Tag := {
  partialEqInst := Tag.Insts.CoreCmpPartialEqTag
  assert_fields_are_eq := Tag.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- [tacenta_protobuf::Reader]
    Source: 'protobuf/src/lib.rs', lines 92:0-97:1
    Visibility: public -/
structure Reader where
  bytes : alloc.vec.Vec Std.U8
  «at» : Std.Usize

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::new]:
    Source: 'protobuf/src/lib.rs', lines 102:4-107:5
    Visibility: public -/
def Reader.new
  (bytes : alloc.vec.Vec Std.U8) :
  Result (core.result.Result Reader ProtoError)
  := do
  let i := alloc.vec.Vec.len bytes
  if i > MAX_MESSAGE_LEN
  then ok (core.result.Result.Err ProtoError.TooLong)
  else ok (core.result.Result.Ok { bytes, «at» := 0#usize })

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::remaining]:
    Source: 'protobuf/src/lib.rs', lines 111:4-117:5
    Visibility: public -/
def Reader.remaining (self : Reader) : Result Std.Usize := do
  let i := alloc.vec.Vec.len self.bytes
  if self.«at» > i
  then ok 0#usize
  else let i1 := alloc.vec.Vec.len self.bytes
       i1 - self.«at»

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::byte]:
    Source: 'protobuf/src/lib.rs', lines 120:4-132:5
    Visibility: public -/
def Reader.byte
  (self : Reader) :
  Result ((core.result.Result Std.U8 ProtoError) × Reader)
  := do
  let i := alloc.vec.Vec.len self.bytes
  if self.«at» >= i
  then ok (core.result.Result.Err ProtoError.Truncated, self)
  else
    let b ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U8)
        self.bytes self.«at»
    let o ← lift (Usize.checked_add self.«at» 1#usize)
    match o with
    | none => ok (core.result.Result.Err ProtoError.TooLong, self)
    | some next => ok (core.result.Result.Ok b, { self with «at» := next })

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::varint]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-206:5
    Visibility: public -/
@[rust_loop_body]
def Reader.varint_loop.body
  (self : Reader) (value : Std.U32) (factor : Std.U32) (taken : Std.Usize) :
  Result (ControlFlow (Reader × Std.U32 × Std.U32 × Std.Usize)
    ((core.result.Result Std.U32 ProtoError) × Reader))
  := do
  if taken < MAX_VARINT_BYTES
  then
    let (r, self1) ← Reader.byte self
    let cf ← core.result.Result.Insts.CoreOpsTry.branch r
    match cf with
    | core.ops.control_flow.ControlFlow.Continue val =>
      let i ← val % 128#u8
      let low ← lift (UScalar.cast .U32 i)
      let o ← lift (U32.checked_mul low factor)
      match o with
      | none => ok (done (core.result.Result.Err ProtoError.BadVarint, self1))
      | some part =>
        let o1 ← lift (U32.checked_add value part)
        match o1 with
        | none =>
          ok (done (core.result.Result.Err ProtoError.BadVarint, self1))
        | some v =>
          let taken1 ← taken + 1#usize
          if val < 128#u8
          then
            if factor > 1#u32
            then
              if val = 0#u8
              then
                ok (done (core.result.Result.Err ProtoError.BadVarint, self1))
              else ok (done (core.result.Result.Ok v, self1))
            else ok (done (core.result.Result.Ok v, self1))
          else
            let o2 ← lift (U32.checked_mul factor 128#u32)
            match o2 with
            | none =>
              ok (done (core.result.Result.Err ProtoError.BadVarint, self1))
            | some f => ok (cont (self1, v, f, taken1))
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r1 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          Std.U32 (core.convert.FromSame ProtoError) residual
      ok (done (r1, self1))
  else ok (done (core.result.Result.Err ProtoError.BadVarint, self))

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::varint]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-206:5
    Visibility: public -/
@[rust_loop]
def Reader.varint_loop
  (self : Reader) (value : Std.U32) (factor : Std.U32) (taken : Std.Usize) :
  Result ((core.result.Result Std.U32 ProtoError) × Reader)
  := do
  loop
    (fun (self1, value1, factor1, taken1) => Reader.varint_loop.body self1
      value1 factor1 taken1)
    (self, value, factor, taken)

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::varint]:
    Source: 'protobuf/src/lib.rs', lines 156:4-206:5
    Visibility: public -/
@[reducible]
def Reader.varint
  (self : Reader) :
  Result ((core.result.Result Std.U32 ProtoError) × Reader)
  := do
  Reader.varint_loop self 0#u32 1#u32 0#usize

/-- [tacenta_protobuf::decode_tag]:
    Source: 'protobuf/src/lib.rs', lines 335:0-358:1
    Visibility: public -/
def decode_tag
  (raw1 : Std.U32) : Result (core.result.Result Tag ProtoError) := do
  let field ← raw1 / 8#u32
  let wire ← raw1 % 8#u32
  if field = 0#u32
  then ok (core.result.Result.Err ProtoError.NotInProfile)
  else
    if field > MAX_FIELD_NUMBER
    then ok (core.result.Result.Err ProtoError.NotInProfile)
    else
      if wire = 0#u32
      then ok (core.result.Result.Ok { field, wire := WireType.Varint })
      else
        if wire = 2#u32
        then
          ok (core.result.Result.Ok
            { field, wire := WireType.LengthDelimited })
        else ok (core.result.Result.Err ProtoError.NotInProfile)

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::tag]:
    Source: 'protobuf/src/lib.rs', lines 215:4-218:5
    Visibility: public -/
def Reader.tag
  (self : Reader) :
  Result ((core.result.Result Tag ProtoError) × Reader)
  := do
  let (r, self1) ← Reader.varint self
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let r1 ← decode_tag val
    ok (r1, self1)
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        Tag (core.convert.FromSame ProtoError) residual
    ok (r1, self1)

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::length_delimited]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 247:8-250:9
    Visibility: public -/
@[rust_loop_body]
def Reader.length_delimited_loop.body
  (v : alloc.vec.Vec Std.U8) (end1 : Std.Usize) (out : alloc.vec.Vec Std.U8)
  (i : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec Std.U8) × Std.Usize) (alloc.vec.Vec
    Std.U8))
  := do
  if i < end1
  then
    let i1 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U8) v i
    let out1 ← alloc.vec.Vec.push out i1
    let i2 ← i + 1#usize
    ok (cont (out1, i2))
  else ok (done out)

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::length_delimited]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 247:8-250:9
    Visibility: public -/
@[rust_loop]
def Reader.length_delimited_loop
  (v : alloc.vec.Vec Std.U8) (end1 : Std.Usize) (out : alloc.vec.Vec Std.U8)
  (i : Std.Usize) :
  Result (alloc.vec.Vec Std.U8)
  := do
  loop
    (fun (out1, i1) => Reader.length_delimited_loop.body v end1 out1 i1)
    (out, i)

/-- [tacenta_protobuf::{tacenta_protobuf::Reader}::length_delimited]:
    Source: 'protobuf/src/lib.rs', lines 226:4-253:5
    Visibility: public -/
def Reader.length_delimited
  (self : Reader) :
  Result ((core.result.Result (alloc.vec.Vec Std.U8) ProtoError) × Reader)
  := do
  let (r, self1) ← Reader.varint self
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let len ← lift (UScalar.cast .Usize val)
    let i ← Reader.remaining self1
    if len > i
    then ok (core.result.Result.Err ProtoError.TooLong, self1)
    else
      let o ← lift (Usize.checked_add self1.«at» len)
      match o with
      | none => ok (core.result.Result.Err ProtoError.TooLong, self1)
      | some e =>
        let out ←
          Reader.length_delimited_loop self1.bytes e (alloc.vec.Vec.new Std.U8)
            self1.«at»
        ok (core.result.Result.Ok out, { self1 with «at» := e })
  | core.ops.control_flow.ControlFlow.Break residual =>
    let r1 ←
      core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
        (alloc.vec.Vec Std.U8) (core.convert.FromSame ProtoError) residual
    ok (r1, self1)

/-- [tacenta_protobuf::FieldSet]
    Source: 'protobuf/src/lib.rs', lines 268:0-270:1
    Visibility: public -/
structure FieldSet where
  seen : alloc.vec.Vec Std.U32

/-- [tacenta_protobuf::{tacenta_protobuf::FieldSet}::new]:
    Source: 'protobuf/src/lib.rs', lines 280:4-282:5
    Visibility: public -/
def FieldSet.new : Result FieldSet := do
  ok { seen := (alloc.vec.Vec.new Std.U32) }

/-- [tacenta_protobuf::{impl core::default::Default for tacenta_protobuf::FieldSet}::default]:
    Source: 'protobuf/src/lib.rs', lines 273:4-275:5
    Visibility: public -/
def FieldSet.Insts.CoreDefaultDefault.default : Result FieldSet := do
  FieldSet.new

/-- Trait implementation: [tacenta_protobuf::{impl core::default::Default for tacenta_protobuf::FieldSet}]
    Source: 'protobuf/src/lib.rs', lines 272:0-276:1 -/
@[reducible]
def FieldSet.Insts.CoreDefaultDefault : core.default.Default FieldSet := {
  default := FieldSet.Insts.CoreDefaultDefault.default
}

/-- [tacenta_protobuf::{tacenta_protobuf::FieldSet}::contains]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 291:8-298:5
    Visibility: public -/
@[rust_loop_body]
def FieldSet.contains_loop.body
  (self : FieldSet) (field : Std.U32) (i : Std.Usize) :
  Result (ControlFlow Std.Usize Bool)
  := do
  let i1 := alloc.vec.Vec.len self.seen
  if i < i1
  then
    let i2 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32)
        self.seen i
    if i2 = field
    then ok (done true)
    else let i3 ← i + 1#usize
         ok (cont i3)
  else ok (done false)

/-- [tacenta_protobuf::{tacenta_protobuf::FieldSet}::contains]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 291:8-298:5
    Visibility: public -/
@[rust_loop]
def FieldSet.contains_loop
  (self : FieldSet) (field : Std.U32) (i : Std.Usize) : Result Bool := do
  loop
    (fun i1 => FieldSet.contains_loop.body self field i1)
    i

/-- [tacenta_protobuf::{tacenta_protobuf::FieldSet}::contains]:
    Source: 'protobuf/src/lib.rs', lines 289:4-298:5
    Visibility: public -/
@[reducible]
def FieldSet.contains (self : FieldSet) (field : Std.U32) : Result Bool := do
  FieldSet.contains_loop self field 0#usize

/-- [tacenta_protobuf::{tacenta_protobuf::FieldSet}::admit]:
    Source: 'protobuf/src/lib.rs', lines 315:4-324:5
    Visibility: public -/
def FieldSet.admit
  (self : FieldSet) (field : Std.U32) :
  Result ((Option ProtoError) × FieldSet)
  := do
  let b ← FieldSet.contains self field
  if b
  then ok (some ProtoError.Duplicate, self)
  else
    let i := alloc.vec.Vec.len self.seen
    if i >= MAX_FIELDS
    then ok (some ProtoError.TooLong, self)
    else let v ← alloc.vec.Vec.push self.seen field
         ok (none, { seen := v })

/-- [tacenta_protobuf::PrekeyBody]
    Source: 'protobuf/src/lib.rs', lines 372:0-384:1
    Visibility: public -/
structure PrekeyBody where
  prekey_id : Option Std.U32
  base_key : alloc.vec.Vec Std.U8
  identity_key : alloc.vec.Vec Std.U8
  message : alloc.vec.Vec Std.U8
  registration_id : Std.U32
  signed_prekey_id : Std.U32
  pq_prekey_id : Std.U32
  kem : alloc.vec.Vec Std.U8

/-- [tacenta_protobuf::FIELD_PREKEY_ID]
    Source: 'protobuf/src/lib.rs', lines 386:0-386:35
    Visibility: public -/
@[global_simps, irreducible] def FIELD_PREKEY_ID : Std.U32 := 1#u32

/-- [tacenta_protobuf::FIELD_BASE_KEY]
    Source: 'protobuf/src/lib.rs', lines 387:0-387:34
    Visibility: public -/
@[global_simps, irreducible] def FIELD_BASE_KEY : Std.U32 := 2#u32

/-- [tacenta_protobuf::FIELD_IDENTITY_KEY]
    Source: 'protobuf/src/lib.rs', lines 388:0-388:38
    Visibility: public -/
@[global_simps, irreducible] def FIELD_IDENTITY_KEY : Std.U32 := 3#u32

/-- [tacenta_protobuf::FIELD_MESSAGE]
    Source: 'protobuf/src/lib.rs', lines 389:0-389:33
    Visibility: public -/
@[global_simps, irreducible] def FIELD_MESSAGE : Std.U32 := 4#u32

/-- [tacenta_protobuf::FIELD_REGISTRATION_ID]
    Source: 'protobuf/src/lib.rs', lines 390:0-390:41
    Visibility: public -/
@[global_simps, irreducible] def FIELD_REGISTRATION_ID : Std.U32 := 5#u32

/-- [tacenta_protobuf::FIELD_SIGNED_PREKEY_ID]
    Source: 'protobuf/src/lib.rs', lines 391:0-391:42
    Visibility: public -/
@[global_simps, irreducible] def FIELD_SIGNED_PREKEY_ID : Std.U32 := 6#u32

/-- [tacenta_protobuf::FIELD_PQ_PREKEY_ID]
    Source: 'protobuf/src/lib.rs', lines 392:0-392:38
    Visibility: public -/
@[global_simps, irreducible] def FIELD_PQ_PREKEY_ID : Std.U32 := 7#u32

/-- [tacenta_protobuf::FIELD_KEM]
    Source: 'protobuf/src/lib.rs', lines 393:0-393:29
    Visibility: public -/
@[global_simps, irreducible] def FIELD_KEM : Std.U32 := 8#u32

/-- [tacenta_protobuf::EnvelopeParse]
    Source: 'protobuf/src/lib.rs', lines 396:0-401:1
    Visibility: public -/
structure EnvelopeParse where
  reader : Reader
  seen : FieldSet
  body : PrekeyBody
  error : Option ProtoError

/-- [tacenta_protobuf::one_envelope_field]:
    Source: 'protobuf/src/lib.rs', lines 404:0-500:1
    Visibility: public -/
def one_envelope_field (st : EnvelopeParse) : Result EnvelopeParse := do
  let (r, r1) ← Reader.tag st.reader
  match r with
  | core.result.Result.Ok t =>
    let (o, fs) ← FieldSet.admit st.seen t.field
    match o with
    | none =>
      if t.field = FIELD_BASE_KEY
      then
        let b ←
          match t.wire with
          | WireType.Varint => ok false
          | WireType.LengthDelimited => ok true
        if b
        then
          let (r2, r3) ← Reader.length_delimited r1
          match r2 with
          | core.result.Result.Ok v =>
            ok
              {
                st
                  with
                  reader := r3,
                  seen := fs,
                  body := { st.body with base_key := v }
              }
          | core.result.Result.Err e =>
            ok { st with reader := r3, seen := fs, error := (some e) }
        else
          ok
            {
              st
                with
                reader := r1,
                seen := fs,
                error := (some ProtoError.NotInProfile)
            }
      else
        if t.field = FIELD_IDENTITY_KEY
        then
          let b ←
            match t.wire with
            | WireType.Varint => ok false
            | WireType.LengthDelimited => ok true
          if b
          then
            let (r2, r3) ← Reader.length_delimited r1
            match r2 with
            | core.result.Result.Ok v =>
              ok
                {
                  st
                    with
                    reader := r3,
                    seen := fs,
                    body := { st.body with identity_key := v }
                }
            | core.result.Result.Err e =>
              ok { st with reader := r3, seen := fs, error := (some e) }
          else
            ok
              {
                st
                  with
                  reader := r1,
                  seen := fs,
                  error := (some ProtoError.NotInProfile)
              }
        else
          if t.field = FIELD_MESSAGE
          then
            let b ←
              match t.wire with
              | WireType.Varint => ok false
              | WireType.LengthDelimited => ok true
            if b
            then
              let (r2, r3) ← Reader.length_delimited r1
              match r2 with
              | core.result.Result.Ok v =>
                ok
                  {
                    st
                      with
                      reader := r3,
                      seen := fs,
                      body := { st.body with message := v }
                  }
              | core.result.Result.Err e =>
                ok { st with reader := r3, seen := fs, error := (some e) }
            else
              ok
                {
                  st
                    with
                    reader := r1,
                    seen := fs,
                    error := (some ProtoError.NotInProfile)
                }
          else
            if t.field = FIELD_KEM
            then
              let b ←
                match t.wire with
                | WireType.Varint => ok false
                | WireType.LengthDelimited => ok true
              if b
              then
                let (r2, r3) ← Reader.length_delimited r1
                match r2 with
                | core.result.Result.Ok v =>
                  ok
                    {
                      st
                        with
                        reader := r3,
                        seen := fs,
                        body := { st.body with kem := v }
                    }
                | core.result.Result.Err e =>
                  ok { st with reader := r3, seen := fs, error := (some e) }
              else
                ok
                  {
                    st
                      with
                      reader := r1,
                      seen := fs,
                      error := (some ProtoError.NotInProfile)
                  }
            else
              if t.field = FIELD_PREKEY_ID
              then
                let b ←
                  match t.wire with
                  | WireType.Varint => ok true
                  | WireType.LengthDelimited => ok false
                if b
                then
                  let (r2, r3) ← Reader.varint r1
                  match r2 with
                  | core.result.Result.Ok v =>
                    ok
                      {
                        st
                          with
                          reader := r3,
                          seen := fs,
                          body := { st.body with prekey_id := (some v) }
                      }
                  | core.result.Result.Err e =>
                    ok { st with reader := r3, seen := fs, error := (some e) }
                else
                  ok
                    {
                      st
                        with
                        reader := r1,
                        seen := fs,
                        error := (some ProtoError.NotInProfile)
                    }
              else
                if t.field = FIELD_REGISTRATION_ID
                then
                  let b ←
                    match t.wire with
                    | WireType.Varint => ok true
                    | WireType.LengthDelimited => ok false
                  if b
                  then
                    let (r2, r3) ← Reader.varint r1
                    match r2 with
                    | core.result.Result.Ok v =>
                      ok
                        {
                          st
                            with
                            reader := r3,
                            seen := fs,
                            body := { st.body with registration_id := v }
                        }
                    | core.result.Result.Err e =>
                      ok
                        { st with reader := r3, seen := fs, error := (some e) }
                  else
                    ok
                      {
                        st
                          with
                          reader := r1,
                          seen := fs,
                          error := (some ProtoError.NotInProfile)
                      }
                else
                  if t.field = FIELD_SIGNED_PREKEY_ID
                  then
                    let b ←
                      match t.wire with
                      | WireType.Varint => ok true
                      | WireType.LengthDelimited => ok false
                    if b
                    then
                      let (r2, r3) ← Reader.varint r1
                      match r2 with
                      | core.result.Result.Ok v =>
                        ok
                          {
                            st
                              with
                              reader := r3,
                              seen := fs,
                              body := { st.body with signed_prekey_id := v }
                          }
                      | core.result.Result.Err e =>
                        ok
                          { st with reader := r3, seen := fs, error := (some e)
                          }
                    else
                      ok
                        {
                          st
                            with
                            reader := r1,
                            seen := fs,
                            error := (some ProtoError.NotInProfile)
                        }
                  else
                    if t.field = FIELD_PQ_PREKEY_ID
                    then
                      let b ←
                        match t.wire with
                        | WireType.Varint => ok true
                        | WireType.LengthDelimited => ok false
                      if b
                      then
                        let (r2, r3) ← Reader.varint r1
                        match r2 with
                        | core.result.Result.Ok v =>
                          ok
                            {
                              st
                                with
                                reader := r3,
                                seen := fs,
                                body := { st.body with pq_prekey_id := v }
                            }
                        | core.result.Result.Err e =>
                          ok
                            {
                              st
                                with
                                reader := r3, seen := fs, error := (some e)
                            }
                      else
                        ok
                          {
                            st
                              with
                              reader := r1,
                              seen := fs,
                              error := (some ProtoError.NotInProfile)
                          }
                    else
                      ok
                        {
                          st
                            with
                            reader := r1,
                            seen := fs,
                            error := (some ProtoError.NotInProfile)
                        }
    | some _ => ok { st with reader := r1, seen := fs, error := o }
  | core.result.Result.Err e => ok { st with reader := r1, error := (some e) }

/-- [tacenta_protobuf::parse_prekey_body]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-529:5
    Visibility: public -/
@[rust_loop_body]
def parse_prekey_body_loop.body
  (st : EnvelopeParse) (turns : Std.Usize) :
  Result (ControlFlow (EnvelopeParse × Std.Usize) EnvelopeParse)
  := do
  if turns < MAX_FIELDS
  then
    let b := core.option.Option.is_none st.error
    if b
    then
      let i ← Reader.remaining st.reader
      if i > 0#usize
      then
        let st1 ← one_envelope_field st
        let turns1 ← turns + 1#usize
        ok (cont (st1, turns1))
      else ok (done st)
    else ok (done st)
  else ok (done st)

/-- [tacenta_protobuf::parse_prekey_body]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-529:5
    Visibility: public -/
@[rust_loop]
def parse_prekey_body_loop
  (st : EnvelopeParse) (turns : Std.Usize) : Result EnvelopeParse := do
  loop
    (fun (st1, turns1) => parse_prekey_body_loop.body st1 turns1)
    (st, turns)

/-- [tacenta_protobuf::parse_prekey_body]:
    Source: 'protobuf/src/lib.rs', lines 503:0-553:1
    Visibility: public -/
def parse_prekey_body
  (bytes : alloc.vec.Vec Std.U8) :
  Result (core.result.Result PrekeyBody ProtoError)
  := do
  let r ← Reader.new bytes
  match r with
  | core.result.Result.Ok r1 =>
    let fs ← FieldSet.new
    let st ←
      parse_prekey_body_loop
        {
          reader := r1,
          seen := fs,
          body :=
            {
              prekey_id := none,
              base_key := (alloc.vec.Vec.new Std.U8),
              identity_key := (alloc.vec.Vec.new Std.U8),
              message := (alloc.vec.Vec.new Std.U8),
              registration_id := 0#u32,
              signed_prekey_id := 0#u32,
              pq_prekey_id := 0#u32,
              kem := (alloc.vec.Vec.new Std.U8)
            },
          error := none
        } 0#usize
    match st.error with
    | none =>
      let i ← Reader.remaining st.reader
      if i > 0#usize
      then ok (core.result.Result.Err ProtoError.TooLong)
      else
        let b ← FieldSet.contains st.seen FIELD_BASE_KEY
        if b
        then
          let b1 ← FieldSet.contains st.seen FIELD_IDENTITY_KEY
          if b1
          then
            let b2 ← FieldSet.contains st.seen FIELD_MESSAGE
            if b2
            then
              let b3 ← FieldSet.contains st.seen FIELD_REGISTRATION_ID
              if b3
              then
                let b4 ← FieldSet.contains st.seen FIELD_SIGNED_PREKEY_ID
                if b4
                then
                  let b5 ← FieldSet.contains st.seen FIELD_PQ_PREKEY_ID
                  if b5
                  then
                    let b6 ← FieldSet.contains st.seen FIELD_KEM
                    if b6
                    then ok (core.result.Result.Ok st.body)
                    else ok (core.result.Result.Err ProtoError.Truncated)
                  else ok (core.result.Result.Err ProtoError.Truncated)
                else ok (core.result.Result.Err ProtoError.Truncated)
              else ok (core.result.Result.Err ProtoError.Truncated)
            else ok (core.result.Result.Err ProtoError.Truncated)
          else ok (core.result.Result.Err ProtoError.Truncated)
        else ok (core.result.Result.Err ProtoError.Truncated)
    | some e => ok (core.result.Result.Err e)
  | core.result.Result.Err e => ok (core.result.Result.Err e)

/-- [tacenta_protobuf::VarintOut]
    Source: 'protobuf/src/lib.rs', lines 561:0-566:1
    Visibility: public -/
structure VarintOut where
  bytes : alloc.vec.Vec Std.U8
  value : Std.U32
  done : Bool
  error : Option ProtoError

/-- [tacenta_protobuf::push_bounded]:
    Source: 'protobuf/src/lib.rs', lines 575:0-581:1
    Visibility: public -/
def push_bounded
  (bytes : alloc.vec.Vec Std.U8) (b : Std.U8) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  let i := alloc.vec.Vec.len bytes
  if i >= MAX_MESSAGE_LEN
  then ok (core.result.Result.Err ProtoError.TooLong)
  else
    let bytes1 ← alloc.vec.Vec.push bytes b
    ok (core.result.Result.Ok bytes1)

/-- [tacenta_protobuf::varint_step]:
    Source: 'protobuf/src/lib.rs', lines 584:0-606:1
    Visibility: public -/
def varint_step (st : VarintOut) : Result VarintOut := do
  let i ← st.value % 128#u32
  let low ← lift (UScalar.cast .U8 i)
  let rest ← st.value / 128#u32
  let b ← if rest = 0#u32
            then ok low
            else low + 128#u8
  let r ← push_bounded st.bytes b
  match r with
  | core.result.Result.Ok next =>
    if rest = 0#u32
    then ok { st with bytes := next, done := true }
    else ok { st with bytes := next, value := rest }
  | core.result.Result.Err e =>
    ok { st with bytes := (alloc.vec.Vec.new Std.U8), error := (some e) }

/-- [tacenta_protobuf::encode_varint]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-625:5
    Visibility: public -/
@[rust_loop_body]
def encode_varint_loop.body
  (st : VarintOut) (turns : Std.Usize) :
  Result (ControlFlow (VarintOut × Std.Usize) VarintOut)
  := do
  if turns < MAX_VARINT_BYTES
  then
    if st.done
    then ok (done st)
    else
      let b := core.option.Option.is_none st.error
      if b
      then
        let st1 ← varint_step st
        let turns1 ← turns + 1#usize
        ok (cont (st1, turns1))
      else ok (done st)
  else ok (done st)

/-- [tacenta_protobuf::encode_varint]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-625:5
    Visibility: public -/
@[rust_loop]
def encode_varint_loop
  (st : VarintOut) (turns : Std.Usize) : Result VarintOut := do
  loop
    (fun (st1, turns1) => encode_varint_loop.body st1 turns1)
    (st, turns)

/-- [tacenta_protobuf::encode_varint]:
    Source: 'protobuf/src/lib.rs', lines 614:0-630:1
    Visibility: public -/
def encode_varint
  (bytes : alloc.vec.Vec Std.U8) (value : Std.U32) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  let st ←
    encode_varint_loop { bytes, value, done := false, error := none } 0#usize
  match st.error with
  | none => ok (core.result.Result.Ok st.bytes)
  | some e => ok (core.result.Result.Err e)

/-- [tacenta_protobuf::wire_code]:
    Source: 'protobuf/src/lib.rs', lines 639:0-645:1
    Visibility: public -/
def wire_code (wire : WireType) : Result Std.U32 := do
  let b ←
    match wire with
    | WireType.Varint => ok true
    | WireType.LengthDelimited => ok false
  if b
  then ok 0#u32
  else ok 2#u32

/-- [tacenta_protobuf::encode_tag]:
    Source: 'protobuf/src/lib.rs', lines 648:0-657:1
    Visibility: public -/
def encode_tag
  (bytes : alloc.vec.Vec Std.U8) (field : Std.U32) (wire : WireType) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  let w ← wire_code wire
  let o ← lift (U32.checked_mul field 8#u32)
  match o with
  | none => ok (core.result.Result.Err ProtoError.NotInProfile)
  | some base =>
    let o1 ← lift (U32.checked_add base w)
    match o1 with
    | none => ok (core.result.Result.Err ProtoError.NotInProfile)
    | some raw1 => encode_varint bytes raw1

/-- [tacenta_protobuf::CopyOut]
    Source: 'protobuf/src/lib.rs', lines 660:0-664:1
    Visibility: public -/
structure CopyOut where
  bytes : alloc.vec.Vec Std.U8
  i : Std.Usize
  error : Option ProtoError

/-- [tacenta_protobuf::copy_step]:
    Source: 'protobuf/src/lib.rs', lines 667:0-683:1
    Visibility: public -/
def copy_step (st : CopyOut) (value : Slice Std.U8) : Result CopyOut := do
  let i := Slice.len value
  if st.i >= i
  then ok { st with error := (some ProtoError.Truncated) }
  else
    let i1 ← Slice.index_usize value st.i
    let r ← push_bounded st.bytes i1
    match r with
    | core.result.Result.Ok next =>
      let i2 ← st.i + 1#usize
      ok { st with bytes := next, i := i2 }
    | core.result.Result.Err e =>
      ok { st with bytes := (alloc.vec.Vec.new Std.U8), error := (some e) }

/-- [tacenta_protobuf::encode_length_delimited]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-703:5
    Visibility: public -/
@[rust_loop_body]
def encode_length_delimited_loop.body
  (value : Slice Std.U8) (st : CopyOut) (turns : Std.Usize) :
  Result (ControlFlow (CopyOut × Std.Usize) CopyOut)
  := do
  if turns < MAX_MESSAGE_LEN
  then
    let i := Slice.len value
    if st.i < i
    then
      let b := core.option.Option.is_none st.error
      if b
      then
        let st1 ← copy_step st value
        let turns1 ← turns + 1#usize
        ok (cont (st1, turns1))
      else ok (done st)
    else ok (done st)
  else ok (done st)

/-- [tacenta_protobuf::encode_length_delimited]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-703:5
    Visibility: public -/
@[rust_loop]
def encode_length_delimited_loop
  (value : Slice Std.U8) (st : CopyOut) (turns : Std.Usize) :
  Result CopyOut
  := do
  loop
    (fun (st1, turns1) => encode_length_delimited_loop.body value st1 turns1)
    (st, turns)

/-- [tacenta_protobuf::encode_length_delimited]:
    Source: 'protobuf/src/lib.rs', lines 686:0-708:1
    Visibility: public -/
def encode_length_delimited
  (bytes : alloc.vec.Vec Std.U8) (value : Slice Std.U8) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  let i := Slice.len value
  if i > MAX_MESSAGE_LEN
  then ok (core.result.Result.Err ProtoError.TooLong)
  else
    let i1 := Slice.len value
    let i2 ← lift (UScalar.cast .U32 i1)
    let r ← encode_varint bytes i2
    match r with
    | core.result.Result.Ok b =>
      let st ←
        encode_length_delimited_loop value
          { bytes := b, i := 0#usize, error := none } 0#usize
      match st.error with
      | none => ok (core.result.Result.Ok st.bytes)
      | some e => ok (core.result.Result.Err e)
    | core.result.Result.Err _ => ok r

/-- [tacenta_protobuf::FIELD_PQ]
    Source: 'protobuf/src/lib.rs', lines 902:0-902:28
    Visibility: public -/
@[global_simps, irreducible] def FIELD_PQ : Std.U32 := 5#u32

/-- [tacenta_protobuf::FIELD_CIPHERTEXT]
    Source: 'protobuf/src/lib.rs', lines 901:0-901:36
    Visibility: public -/
@[global_simps, irreducible] def FIELD_CIPHERTEXT : Std.U32 := 4#u32

/-- [tacenta_protobuf::FIELD_PREVIOUS_COUNTER]
    Source: 'protobuf/src/lib.rs', lines 900:0-900:42
    Visibility: public -/
@[global_simps, irreducible] def FIELD_PREVIOUS_COUNTER : Std.U32 := 3#u32

/-- [tacenta_protobuf::FIELD_COUNTER]
    Source: 'protobuf/src/lib.rs', lines 899:0-899:33
    Visibility: public -/
@[global_simps, irreducible] def FIELD_COUNTER : Std.U32 := 2#u32

/-- [tacenta_protobuf::FIELD_RATCHET_KEY]
    Source: 'protobuf/src/lib.rs', lines 898:0-898:37
    Visibility: public -/
@[global_simps, irreducible] def FIELD_RATCHET_KEY : Std.U32 := 1#u32

/-- [tacenta_protobuf::RatchetBody]
    Source: 'protobuf/src/lib.rs', lines 889:0-895:1
    Visibility: public -/
structure RatchetBody where
  ratchet_key : alloc.vec.Vec Std.U8
  counter : Std.U32
  previous_counter : Std.U32
  ciphertext : alloc.vec.Vec Std.U8
  pq : alloc.vec.Vec Std.U8

/-- [tacenta_protobuf::encode_ratchet_body]:
    Source: 'protobuf/src/lib.rs', lines 728:0-767:1
    Visibility: public -/
def encode_ratchet_body
  (body : RatchetBody) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  let r ←
    encode_tag (alloc.vec.Vec.new Std.U8) FIELD_RATCHET_KEY
      WireType.LengthDelimited
  match r with
  | core.result.Result.Ok b =>
    let s := alloc.vec.Vec.deref body.ratchet_key
    let r1 ← encode_length_delimited b s
    match r1 with
    | core.result.Result.Ok b1 =>
      let r2 ← encode_tag b1 FIELD_COUNTER WireType.Varint
      match r2 with
      | core.result.Result.Ok b2 =>
        let r3 ← encode_varint b2 body.counter
        match r3 with
        | core.result.Result.Ok b3 =>
          let r4 ← encode_tag b3 FIELD_PREVIOUS_COUNTER WireType.Varint
          match r4 with
          | core.result.Result.Ok b4 =>
            let r5 ← encode_varint b4 body.previous_counter
            match r5 with
            | core.result.Result.Ok b5 =>
              let r6 ←
                encode_tag b5 FIELD_CIPHERTEXT WireType.LengthDelimited
              match r6 with
              | core.result.Result.Ok b6 =>
                let s1 := alloc.vec.Vec.deref body.ciphertext
                let r7 ← encode_length_delimited b6 s1
                match r7 with
                | core.result.Result.Ok b7 =>
                  let r8 ← encode_tag b7 FIELD_PQ WireType.LengthDelimited
                  match r8 with
                  | core.result.Result.Ok b8 =>
                    let s2 := alloc.vec.Vec.deref body.pq
                    encode_length_delimited b8 s2
                  | core.result.Result.Err _ => ok r8
                | core.result.Result.Err _ => ok r7
              | core.result.Result.Err _ => ok r6
            | core.result.Result.Err _ => ok r5
          | core.result.Result.Err _ => ok r4
        | core.result.Result.Err _ => ok r3
      | core.result.Result.Err _ => ok r2
    | core.result.Result.Err _ => ok r1
  | core.result.Result.Err _ => ok r

/-- [tacenta_protobuf::encode_prekey_body]:
    Source: 'protobuf/src/lib.rs', lines 776:0-846:1
    Visibility: public -/
def encode_prekey_body
  (body : PrekeyBody) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  match body.prekey_id with
  | none =>
    let r ←
      encode_tag (alloc.vec.Vec.new Std.U8) FIELD_BASE_KEY
        WireType.LengthDelimited
    match r with
    | core.result.Result.Ok b =>
      let s := alloc.vec.Vec.deref body.base_key
      let r1 ← encode_length_delimited b s
      match r1 with
      | core.result.Result.Ok b1 =>
        let r2 ← encode_tag b1 FIELD_IDENTITY_KEY WireType.LengthDelimited
        match r2 with
        | core.result.Result.Ok b2 =>
          let s1 := alloc.vec.Vec.deref body.identity_key
          let r3 ← encode_length_delimited b2 s1
          match r3 with
          | core.result.Result.Ok b3 =>
            let r4 ← encode_tag b3 FIELD_MESSAGE WireType.LengthDelimited
            match r4 with
            | core.result.Result.Ok b4 =>
              let s2 := alloc.vec.Vec.deref body.message
              let r5 ← encode_length_delimited b4 s2
              match r5 with
              | core.result.Result.Ok b5 =>
                let r6 ← encode_tag b5 FIELD_REGISTRATION_ID WireType.Varint
                match r6 with
                | core.result.Result.Ok b6 =>
                  let r7 ← encode_varint b6 body.registration_id
                  match r7 with
                  | core.result.Result.Ok b7 =>
                    let r8 ←
                      encode_tag b7 FIELD_SIGNED_PREKEY_ID WireType.Varint
                    match r8 with
                    | core.result.Result.Ok b8 =>
                      let r9 ← encode_varint b8 body.signed_prekey_id
                      match r9 with
                      | core.result.Result.Ok b9 =>
                        let r10 ←
                          encode_tag b9 FIELD_PQ_PREKEY_ID WireType.Varint
                        match r10 with
                        | core.result.Result.Ok b10 =>
                          let r11 ← encode_varint b10 body.pq_prekey_id
                          match r11 with
                          | core.result.Result.Ok b11 =>
                            let r12 ←
                              encode_tag b11 FIELD_KEM WireType.LengthDelimited
                            match r12 with
                            | core.result.Result.Ok b12 =>
                              let s3 := alloc.vec.Vec.deref body.kem
                              encode_length_delimited b12 s3
                            | core.result.Result.Err _ => ok r12
                          | core.result.Result.Err _ => ok r11
                        | core.result.Result.Err _ => ok r10
                      | core.result.Result.Err _ => ok r9
                    | core.result.Result.Err _ => ok r8
                  | core.result.Result.Err _ => ok r7
                | core.result.Result.Err _ => ok r6
              | core.result.Result.Err _ => ok r5
            | core.result.Result.Err _ => ok r4
          | core.result.Result.Err _ => ok r3
        | core.result.Result.Err _ => ok r2
      | core.result.Result.Err _ => ok r1
    | core.result.Result.Err _ => ok r
  | some id =>
    let r ←
      encode_tag (alloc.vec.Vec.new Std.U8) FIELD_PREKEY_ID WireType.Varint
    match r with
    | core.result.Result.Ok b =>
      let r1 ← encode_varint b id
      match r1 with
      | core.result.Result.Ok b1 =>
        let r2 ← encode_tag b1 FIELD_BASE_KEY WireType.LengthDelimited
        match r2 with
        | core.result.Result.Ok b2 =>
          let s := alloc.vec.Vec.deref body.base_key
          let r3 ← encode_length_delimited b2 s
          match r3 with
          | core.result.Result.Ok b3 =>
            let r4 ←
              encode_tag b3 FIELD_IDENTITY_KEY WireType.LengthDelimited
            match r4 with
            | core.result.Result.Ok b4 =>
              let s1 := alloc.vec.Vec.deref body.identity_key
              let r5 ← encode_length_delimited b4 s1
              match r5 with
              | core.result.Result.Ok b5 =>
                let r6 ← encode_tag b5 FIELD_MESSAGE WireType.LengthDelimited
                match r6 with
                | core.result.Result.Ok b6 =>
                  let s2 := alloc.vec.Vec.deref body.message
                  let r7 ← encode_length_delimited b6 s2
                  match r7 with
                  | core.result.Result.Ok b7 =>
                    let r8 ←
                      encode_tag b7 FIELD_REGISTRATION_ID WireType.Varint
                    match r8 with
                    | core.result.Result.Ok b8 =>
                      let r9 ← encode_varint b8 body.registration_id
                      match r9 with
                      | core.result.Result.Ok b9 =>
                        let r10 ←
                          encode_tag b9 FIELD_SIGNED_PREKEY_ID WireType.Varint
                        match r10 with
                        | core.result.Result.Ok b10 =>
                          let r11 ← encode_varint b10 body.signed_prekey_id
                          match r11 with
                          | core.result.Result.Ok b11 =>
                            let r12 ←
                              encode_tag b11 FIELD_PQ_PREKEY_ID WireType.Varint
                            match r12 with
                            | core.result.Result.Ok b12 =>
                              let r13 ← encode_varint b12 body.pq_prekey_id
                              match r13 with
                              | core.result.Result.Ok b13 =>
                                let r14 ←
                                  encode_tag b13 FIELD_KEM
                                    WireType.LengthDelimited
                                match r14 with
                                | core.result.Result.Ok b14 =>
                                  let s3 := alloc.vec.Vec.deref body.kem
                                  encode_length_delimited b14 s3
                                | core.result.Result.Err _ => ok r14
                              | core.result.Result.Err _ => ok r13
                            | core.result.Result.Err _ => ok r12
                          | core.result.Result.Err _ => ok r11
                        | core.result.Result.Err _ => ok r10
                      | core.result.Result.Err _ => ok r9
                    | core.result.Result.Err _ => ok r8
                  | core.result.Result.Err _ => ok r7
                | core.result.Result.Err _ => ok r6
              | core.result.Result.Err _ => ok r5
            | core.result.Result.Err _ => ok r4
          | core.result.Result.Err _ => ok r3
        | core.result.Result.Err _ => ok r2
      | core.result.Result.Err _ => ok r1
    | core.result.Result.Err _ => ok r

/-- [tacenta_protobuf::Raw]
    Source: 'protobuf/src/lib.rs', lines 855:0-858:1
    Visibility: public -/
structure Raw where
  start : Std.Usize
  «end» : Std.Usize

/-- [tacenta_protobuf::{tacenta_protobuf::Raw}::slice_of]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 869:8-872:9
    Visibility: public -/
@[rust_loop_body]
def Raw.slice_of_loop.body
  (i : Std.Usize) (bytes : Slice Std.U8) (out : alloc.vec.Vec Std.U8)
  (i1 : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec Std.U8) × Std.Usize) (alloc.vec.Vec
    Std.U8))
  := do
  if i1 < i
  then
    let i2 ← Slice.index_usize bytes i1
    let out1 ← alloc.vec.Vec.push out i2
    let i3 ← i1 + 1#usize
    ok (cont (out1, i3))
  else ok (done out)

/-- [tacenta_protobuf::{tacenta_protobuf::Raw}::slice_of]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 869:8-872:9
    Visibility: public -/
@[rust_loop]
def Raw.slice_of_loop
  (i : Std.Usize) (bytes : Slice Std.U8) (out : alloc.vec.Vec Std.U8)
  (i1 : Std.Usize) :
  Result (alloc.vec.Vec Std.U8)
  := do
  loop
    (fun (out1, i2) => Raw.slice_of_loop.body i bytes out1 i2)
    (out, i1)

/-- [tacenta_protobuf::{tacenta_protobuf::Raw}::slice_of]:
    Source: 'protobuf/src/lib.rs', lines 863:4-874:5
    Visibility: public -/
def Raw.slice_of
  (self : Raw) (bytes : Slice Std.U8) :
  Result (core.result.Result (alloc.vec.Vec Std.U8) ProtoError)
  := do
  if self.start > self.«end»
  then ok (core.result.Result.Err ProtoError.Truncated)
  else
    let i := Slice.len bytes
    if self.«end» > i
    then ok (core.result.Result.Err ProtoError.Truncated)
    else
      let out ←
        Raw.slice_of_loop self.«end» bytes (alloc.vec.Vec.new Std.U8)
          self.start
      ok (core.result.Result.Ok out)

/-- [tacenta_protobuf::Parse]
    Source: 'protobuf/src/lib.rs', lines 911:0-920:1
    Visibility: public -/
structure Parse where
  reader : Reader
  seen : FieldSet
  ratchet_key : alloc.vec.Vec Std.U8
  counter : Std.U32
  previous_counter : Std.U32
  ciphertext : alloc.vec.Vec Std.U8
  pq : alloc.vec.Vec Std.U8
  error : Option ProtoError

/-- [tacenta_protobuf::one_field]:
    Source: 'protobuf/src/lib.rs', lines 928:0-995:1
    Visibility: public -/
def one_field (st : Parse) : Result Parse := do
  let (r, r1) ← Reader.tag st.reader
  match r with
  | core.result.Result.Ok t =>
    let (o, fs) ← FieldSet.admit st.seen t.field
    match o with
    | none =>
      if t.field = FIELD_RATCHET_KEY
      then
        let b ←
          match t.wire with
          | WireType.Varint => ok false
          | WireType.LengthDelimited => ok true
        if b
        then
          let (r2, r3) ← Reader.length_delimited r1
          match r2 with
          | core.result.Result.Ok v =>
            ok { st with reader := r3, seen := fs, ratchet_key := v }
          | core.result.Result.Err e =>
            ok { st with reader := r3, seen := fs, error := (some e) }
        else
          ok
            {
              st
                with
                reader := r1,
                seen := fs,
                error := (some ProtoError.NotInProfile)
            }
      else
        if t.field = FIELD_COUNTER
        then
          let b ←
            match t.wire with
            | WireType.Varint => ok true
            | WireType.LengthDelimited => ok false
          if b
          then
            let (r2, r3) ← Reader.varint r1
            match r2 with
            | core.result.Result.Ok v =>
              ok { st with reader := r3, seen := fs, counter := v }
            | core.result.Result.Err e =>
              ok { st with reader := r3, seen := fs, error := (some e) }
          else
            ok
              {
                st
                  with
                  reader := r1,
                  seen := fs,
                  error := (some ProtoError.NotInProfile)
              }
        else
          if t.field = FIELD_PREVIOUS_COUNTER
          then
            let b ←
              match t.wire with
              | WireType.Varint => ok true
              | WireType.LengthDelimited => ok false
            if b
            then
              let (r2, r3) ← Reader.varint r1
              match r2 with
              | core.result.Result.Ok v =>
                ok { st with reader := r3, seen := fs, previous_counter := v }
              | core.result.Result.Err e =>
                ok { st with reader := r3, seen := fs, error := (some e) }
            else
              ok
                {
                  st
                    with
                    reader := r1,
                    seen := fs,
                    error := (some ProtoError.NotInProfile)
                }
          else
            if t.field = FIELD_CIPHERTEXT
            then
              let b ←
                match t.wire with
                | WireType.Varint => ok false
                | WireType.LengthDelimited => ok true
              if b
              then
                let (r2, r3) ← Reader.length_delimited r1
                match r2 with
                | core.result.Result.Ok v =>
                  ok { st with reader := r3, seen := fs, ciphertext := v }
                | core.result.Result.Err e =>
                  ok { st with reader := r3, seen := fs, error := (some e) }
              else
                ok
                  {
                    st
                      with
                      reader := r1,
                      seen := fs,
                      error := (some ProtoError.NotInProfile)
                  }
            else
              if t.field = FIELD_PQ
              then
                let b ←
                  match t.wire with
                  | WireType.Varint => ok false
                  | WireType.LengthDelimited => ok true
                if b
                then
                  let (r2, r3) ← Reader.length_delimited r1
                  match r2 with
                  | core.result.Result.Ok v =>
                    ok { st with reader := r3, seen := fs, pq := v }
                  | core.result.Result.Err e =>
                    ok { st with reader := r3, seen := fs, error := (some e) }
                else
                  ok
                    {
                      st
                        with
                        reader := r1,
                        seen := fs,
                        error := (some ProtoError.NotInProfile)
                    }
              else
                ok
                  {
                    st
                      with
                      reader := r1,
                      seen := fs,
                      error := (some ProtoError.NotInProfile)
                  }
    | some _ => ok { st with reader := r1, seen := fs, error := o }
  | core.result.Result.Err e => ok { st with reader := r1, error := (some e) }

/-- [tacenta_protobuf::parse_ratchet_body]: loop body 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-1028:5
    Visibility: public -/
@[rust_loop_body]
def parse_ratchet_body_loop.body
  (st : Parse) (turns : Std.Usize) :
  Result (ControlFlow (Parse × Std.Usize) Parse)
  := do
  if turns < MAX_FIELDS
  then
    let b := core.option.Option.is_none st.error
    if b
    then
      let i ← Reader.remaining st.reader
      if i > 0#usize
      then
        let st1 ← one_field st
        let turns1 ← turns + 1#usize
        ok (cont (st1, turns1))
      else ok (done st)
    else ok (done st)
  else ok (done st)

/-- [tacenta_protobuf::parse_ratchet_body]: loop 0:
    Source: 'protobuf/src/lib.rs', lines 1:0-1028:5
    Visibility: public -/
@[rust_loop]
def parse_ratchet_body_loop
  (st : Parse) (turns : Std.Usize) : Result Parse := do
  loop
    (fun (st1, turns1) => parse_ratchet_body_loop.body st1 turns1)
    (st, turns)

/-- [tacenta_protobuf::parse_ratchet_body]:
    Source: 'protobuf/src/lib.rs', lines 1007:0-1059:1
    Visibility: public -/
def parse_ratchet_body
  (bytes : alloc.vec.Vec Std.U8) :
  Result (core.result.Result RatchetBody ProtoError)
  := do
  let r ← Reader.new bytes
  match r with
  | core.result.Result.Ok r1 =>
    let fs ← FieldSet.new
    let st ←
      parse_ratchet_body_loop
        {
          reader := r1,
          seen := fs,
          ratchet_key := (alloc.vec.Vec.new Std.U8),
          counter := 0#u32,
          previous_counter := 0#u32,
          ciphertext := (alloc.vec.Vec.new Std.U8),
          pq := (alloc.vec.Vec.new Std.U8),
          error := none
        } 0#usize
    match st.error with
    | none =>
      let i ← Reader.remaining st.reader
      if i > 0#usize
      then ok (core.result.Result.Err ProtoError.TooLong)
      else
        let b ← FieldSet.contains st.seen FIELD_RATCHET_KEY
        if b
        then
          let b1 ← FieldSet.contains st.seen FIELD_COUNTER
          if b1
          then
            let b2 ← FieldSet.contains st.seen FIELD_PREVIOUS_COUNTER
            if b2
            then
              let b3 ← FieldSet.contains st.seen FIELD_CIPHERTEXT
              if b3
              then
                let b4 ← FieldSet.contains st.seen FIELD_PQ
                if b4
                then
                  ok (core.result.Result.Ok
                    {
                      ratchet_key := st.ratchet_key,
                      counter := st.counter,
                      previous_counter := st.previous_counter,
                      ciphertext := st.ciphertext,
                      pq := st.pq
                    })
                else ok (core.result.Result.Err ProtoError.Truncated)
              else ok (core.result.Result.Err ProtoError.Truncated)
            else ok (core.result.Result.Err ProtoError.Truncated)
          else ok (core.result.Result.Err ProtoError.Truncated)
        else ok (core.result.Result.Err ProtoError.Truncated)
    | some e => ok (core.result.Result.Err e)
  | core.result.Result.Err e => ok (core.result.Result.Err e)

end tacenta_protobuf
